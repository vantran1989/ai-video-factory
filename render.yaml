services:
  - type: web
    name: ai-video-factory
    runtime: node
    plan: free

    buildCommand: npm install

    startCommand: npm start

    envVars:
      - key: NODE_ENV
        value: production

      - key: MAX_UPLOAD_MB
        value: "500"
